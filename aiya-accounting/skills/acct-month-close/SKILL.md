---
name: acct-month-close
description: >-
  เช็คลิสต์ปิดงวด รวมภาษีซื้อที่ไม่มีเอกสาร ค่ารับรองที่เคลมผิด และรายการค้างกระทบยอด ก่อนคนกดปิดงวดเอง
  ใช้เมื่อผู้ใช้บอกว่า "จะปิดเดือนแล้ว", "เช็คก่อนปิดงวด", "งวดนี้มีอะไรค้างไหม", "month close checklist".
  Triggers: month close, period close checklist, VAT close, trial balance.
---

# ปิดเดือน

อ้างอิง [PLAN-ACCOUNTING.md](../../../../PLAN-ACCOUNTING.md) §4ก และ
[references/acct-team-rules.md](../../references/acct-team-rules.md)

## ขั้นตอน

1. ถามงวดที่จะปิด และเช็ค `acct_tax_profile_get` ว่ากิจการนี้จด VAT ไหม
2. `acct_vat_report` + `acct_vat_lines` ดูภาษีซื้อ/ขายของงวด
3. `acct_reconcile` ดูค้างทั้งหมดของงวด (บัญชีไหนยังไม่กระทบยอด)
4. 🔴 **acct_gl_trial_balance (งบทดลอง) ยังไม่มี tool วันนี้** (SPEC §1ฉ ไม่มี `gl_*` เลยสักตัวใน `mcp.ts`)
   ถ้าถูกถามงบทดลอง/งบกำไรขาดทุน/งบดุล ให้บอกตรงๆ ว่าเครื่องมือนี้ยังไม่มี **ห้ามคำนวณงบเองจากตารางอื่น**
   ให้ใช้ผล `acct_vat_report` + `acct_reconcile` เป็นเช็คลิสต์แทนได้เท่าที่มีข้อมูล

## ข้อมูลที่ต้องถาม

งวดที่จะปิด (เดือน/ปี) · กิจการนี้จด VAT ไหม (จาก `acct_tax_profile_get` ถ้ายังไม่ตั้งให้ถาม ห้ามเดา)

## รูปแบบผลลัพธ์

เช็คลิสต์ปิดงวด:

```
🗓 งวด <เดือน/ปี> · จด VAT: <ใช่/ไม่/ยังไม่ตั้ง>
🧾 ภาษีซื้อไม่มีเอกสาร: <n> รายการ
🍽 ค่ารับรองที่เคลมผิด: <n> รายการ
🔗 รายการค้างกระทบยอด: <n> รายการ
📊 งบทดลอง: ยังไม่มีเครื่องมือให้ดึง (รอ acct_gl_trial_balance)
👉 ไม่ปิดงวดเอง · ให้คนกด ar_run_close หลังตรวจครบ [ต้องให้คนยืนยัน]
```

**ไม่ปิดงวดเอง** (`ar_run_close` ต้องผ่านประตูอนุมัติ ไม่ใช่หน้าที่ของสกิลนี้)

## ประตูอนุมัติ

สกิลนี้อ่านอย่างเดียวทั้งหมด ไม่มี tool เขียนสักตัว การปิดงวดจริง (`ar_run_close`) อยู่ในด่าน hook ชั้น 1
(**ต้อง** · แก้ยอดที่บันทึกแล้ว) และไม่ได้เรียกจากสกิลนี้

## เดวิลส์แอดโวเคตการเงิน

- **ตัวเลขที่ต่างกันมากโดยไม่มีคำอธิบาย จะถูกเดาเหตุผลใส่เสมอ และมักเดาผิด** (บทเรียนจริงเรื่อง LINE AGP
  principal · PLAN-ACCOUNTING §4ก) **ห้ามสรุปเหตุผลเอง ให้ถามหรือยกธง**
- เอกสารภาษีซื้อที่หาไม่เจอ อย่าสรุปว่า "น่าจะเคลมไม่ได้" ให้รายงานเป็นรายการที่ต้องตามหาก่อน
- งวดที่ยังไม่ตั้งโปรไฟล์ภาษี ห้ามสันนิษฐานว่าไม่จด VAT ต้องถามหรือยกธงว่า "ยังไม่รู้"

## เครื่องมือ MCP ที่สกิลนี้ใช้
<!-- mcp-tools -->
- `acct_tax_profile_get`
- `acct_vat_report`
- `acct_vat_lines`
- `acct_reconcile`
- `ar_run_close`
<!-- /mcp-tools -->

acct_gl_trial_balance ยังไม่ขึ้นทะเบียนเป็น MCP tool (SPEC §1ฉ) · เพิ่มลงตารางข้างบนเมื่อสร้างเสร็จ
